import { config } from '@/lib/config'
export default function Docs(){return <div className="container" style={{padding:'5rem 0 8rem'}}><div className="panel" style={{padding:'clamp(26px,7vw,72px)',maxWidth:800}}><p className="eyebrow">Handoff & reference</p><h1 style={{fontSize:'clamp(2.5rem,7vw,5.5rem)',lineHeight:.95,letterSpacing:'-.07em',margin:'.7rem 0 1.2rem'}}>Build the signal<br/><span style={{color:'var(--accent)'}}>together.</span></h1><p className="muted" style={{fontSize:'1.1rem',maxWidth:580}}>The Unified-Live BookStack wiki is the source of truth for setup notes, operating guides, and community handoff material.</p>{config.docsUrl?<a href={config.docsUrl} target="_blank" rel="noreferrer" className="focus-ring" style={{display:'inline-block',marginTop:28,padding:'13px 18px',borderRadius:12,background:'var(--accent)',color:'#08110e',fontWeight:800}}>Open the BookStack wiki ↗</a>:<p className="muted" style={{marginTop:28}}>The wiki link will appear once <code>NEXT_PUBLIC_DOCS_URL</code> is configured.</p>}</div></div>}
