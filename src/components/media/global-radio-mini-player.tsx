'use client'; import { useMedia } from './media-provider'; import { NowPlaying } from './now-playing'
export function GlobalRadioMiniPlayer(){const {track,mediaStatus}=useMedia();if(mediaStatus==='idle'||mediaStatus==='error')return null;return <aside aria-label="Persistent radio player" style={{position:'fixed',bottom:16,left:'50%',transform:'translateX(-50%)',zIndex:40,width:'min(680px,calc(100% - 2rem))',padding:'10px 14px',borderRadius:16,background:'rgba(16,23,34,.94)',border:'1px solid var(--line)',boxShadow:'0 15px 50px rgba(0,0,0,.35)',backdropFilter:'blur(16px)'}}><NowPlaying track={track}/></aside>}
