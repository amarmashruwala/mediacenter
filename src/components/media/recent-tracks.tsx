import Image from 'next/image'; import type { NowPlaying } from '@/lib/media-types'
export function RecentTracks({data}:{data:NowPlaying|null}){const history=data?.song_history??[];return <section className="panel" style={{padding:24}}><div style={{display:'flex',justifyContent:'space-between',gap:16,alignItems:'baseline'}}><div><p className="eyebrow">History</p><h2 style={{margin:'.35rem 0 0',fontSize:'1.5rem'}}>Recently played</h2></div><span className="muted" style={{fontSize:'.85rem'}}>Last five tracks</span></div><div style={{display:'grid',gap:10,marginTop:18}}>{history.length?history.map(row=><div key={row.sh_id} style={{display:'flex',alignItems:'center',gap:12,padding:'10px 0',borderTop:'1px solid var(--line)'}}>{row.song.art?<Image src={row.song.art} alt="" width={42} height={42} style={{borderRadius:9,objectFit:'cover'}}/>:<div style={{width:42,height:42,borderRadius:9,background:'var(--panel2)'}}/>}<div style={{minWidth:0}}><strong style={{display:'block',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{row.song.title??row.song.text}</strong><span className="muted" style={{fontSize:'.85rem'}}>{row.song.artist}</span></div></div>):<p className="muted">Track history is not available yet.</p>}</div></section>}
