import type { ScheduleEntry } from '@/lib/media-types'
export function StationSchedule({entries}:{entries:ScheduleEntry[]}){const fmt=(value:string)=>new Intl.DateTimeFormat('en-AU',{timeZone:'Australia/Sydney',weekday:'short',hour:'numeric',minute:'2-digit',timeZoneName:'short'}).format(new Date(value));return <section className="panel" style={{padding:24}}><p className="eyebrow">On air</p><h2 style={{margin:'.35rem 0 1.2rem',fontSize:'1.5rem'}}>Station schedule</h2><div style={{display:'grid',gap:12}}>{entries.map(e=><div key={e.id} style={{display:'flex',justifyContent:'space-between',gap:14,padding:'12px 0',borderTop:'1px solid var(--line)',color:e.is_now?'var(--accent)':'var(--text)'}}><div><strong>{e.title}</strong><p className="muted" style={{margin:3, fontSize:'.82rem'}}>{e.description}</p></div><time style={{whiteSpace:'nowrap',fontSize:'.82rem'}}>{fmt(e.start)}<br/>→ {fmt(e.end)}</time></div>)}</div></section>}
