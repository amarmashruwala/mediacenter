import Image from 'next/image'; import { RadioControls } from './radio-controls'; import type { Track } from '@/lib/media-types'
export function NowPlaying({track,showControls=true}:{track?:Track;showControls?:boolean}){return <div style={{display:'flex',alignItems:'center',gap:14,minWidth:0}}>{track?.art?<Image src={track.art} alt="" width={56} height={56} style={{borderRadius:12,objectFit:'cover'}}/>:<div style={{width:56,height:56,borderRadius:12,background:'linear-gradient(135deg,var(--accent),#4778ff)',display:'grid',placeItems:'center',fontWeight:900}}>MC</div>}<div style={{minWidth:0,flex:1}}><p className="eyebrow" style={{margin:'0 0 4px'}}>Now playing</p><p style={{margin:0,fontWeight:800,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{track?.title??track?.text??'Unified-Live Radio'}</p><p className="muted" style={{margin:'3px 0 0',fontSize:'.88rem',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{track?.artist??'Live community broadcast'}</p></div>{showControls&&<RadioControls compact/>}</div>}
