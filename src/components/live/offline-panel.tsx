export function OfflinePanel(){return <div className="panel" style={{padding:'2.5rem',minHeight:240,display:'grid',placeItems:'center',textAlign:'center'}}><div><span className="eyebrow" style={{color:'var(--pink)'}}>Currently offline</span><h2 style={{fontSize:'clamp(1.8rem,4vw,3rem)',margin:'.6rem 0'}}>The live room is quiet.</h2><p className="muted" style={{maxWidth:470,margin:'0 auto'}}>Tune into Unified-Live Radio while the next broadcast is getting ready.</p></div></div>}
